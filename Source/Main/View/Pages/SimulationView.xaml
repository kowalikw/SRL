<UserControl x:Class="SRL.Main.View.Pages.SimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:mga="clr-namespace:SRL.Main.View.MonoGameArea"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="1200"
             DataContext="{Binding Source={StaticResource Locator}, Path=Simulation}">
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <mga:VisualizationArea SnapsToDevicePixels="True"
                               Stretch="Uniform"/>

        <DockPanel Grid.Row="1" LastChildFill="True">
            <DockPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource IconButtonStyle}"/>
                <Style TargetType="ToggleButton" BasedOn="{StaticResource IconToggleButtonStyle}"/>
            </DockPanel.Resources>
            
            <Button Visibility="Collapsed">Start</Button>
            <Button Visibility="Collapsed">Pause</Button>
            <Button>Stop</Button>
            
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Command="{Binding LoadCommand}"
                        Content="{StaticResource LoadFileIcon}"/>
                <Button Command="{Binding SaveCommand}"
                        Content="{StaticResource SaveFileIcon}"/>
                <Button Command="{Binding LoadMapCommand}"/>
                <Button Command="{Binding LoadVehicleCommand}"/>
                <ToggleButton>SetVehiclePosition</ToggleButton>
                <ToggleButton>SetStartEndPoint</ToggleButton>
                <ToggleButton>AA</ToggleButton>
            </StackPanel>
            
            <Slider/>
        </DockPanel>
    </Grid>
</UserControl>
