<UserControl x:Class="SRL.Main.View.Pages.MapEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:mga="clr-namespace:SRL.Main.View.MonoGameArea"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Source={StaticResource Locator}, Path=MapEditor}">

    <Grid Style="{StaticResource ContentRoot}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Button>$load</Button>
            <Button Command="{Binding SaveCommand}">$save</Button>
            <Button>$trace</Button>
            <Button Command="{Binding BackCommand}">$back</Button>
            <Button Command="{Binding ResetCommand}">$reset</Button>
            <ToggleButton IsChecked="{Binding AntialiasingEnabled}">$AA</ToggleButton>
        </StackPanel>
        
        <mga:MapEditArea Grid.Column="1"
                         SnapsToDevicePixels="True"
                         Stretch="Fill"/>
    </Grid>

</UserControl>
