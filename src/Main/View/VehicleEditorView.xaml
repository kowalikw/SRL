<Window x:Class="SRL.Main.View.VehicleEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SRL.Main"
        xmlns:control="clr-namespace:SRL.Main.View.Control"
        mc:Ignorable="d"
        Title="VehicleEditorView" Height="480" Width="640">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <control:VehicleEditArea x:Name="VehicleEditorControl"
                         Grid.Row="0"
                         Grid.Column="0"
                         Margin="5"
                         SnapsToDevicePixels="True"
                         Stretch="Fill"/>
        <StackPanel Grid.Column="1">
            <Button Margin="3" Height="30">Resetuj pojazd</Button>
            <Button Margin="3" Height="30">Zapisz pojazd</Button>
        </StackPanel>
    </Grid>
</Window>
