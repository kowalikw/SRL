<Window x:Class="SRL.Main.View.MainMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SRL.Main"
        xmlns:control="clr-namespace:SRL.MonoGameControl;assembly=SRL.MonoGameControl"
        mc:Ignorable="d"
        Title="MainMenuView" Height="150" Width="525">
    <Grid>
        <StackPanel>
            <Button Command="{Binding VehicleEditorCommand}">Dodaj pojazd</Button>
            <Button Command="{Binding MapEditorCommand}">Dodaj mapę</Button>
        </StackPanel>

    </Grid>
</Window>
