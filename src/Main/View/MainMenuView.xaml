<Window x:Class="SRL.Main.View.MainMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SRL.Main"
        xmlns:control="clr-namespace:SRL.MonoGameControl;assembly=SRL.MonoGameControl"
        mc:Ignorable="d"
        Title="MainMenuView" Height="150" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" x:Name="addVehicle" Click="addVehicle_Click">Dodaj pojazd</Button>
        <Button Grid.Column="1" Grid.Row="0" x:Name="addMap" Click="addMap_Click">Dodaj mapę</Button>
    </Grid>
</Window>
